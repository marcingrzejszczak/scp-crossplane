apiVersion: v1
kind: Service
metadata:
  name: sshd
  namespace: default
  labels:
    app: sshd
spec:
  selector:
    app: sshd
  ports:
    - name: ssh
      port: 2222
      targetPort: 2222
      protocol: TCP
